package conditionalstatement;

public class elseif {  // in our code if statement always checks that means if we take two if statement in our code then
    //both of the two  if statemnt is checked
    // if we want to avoid this then we can take else if operator because if if statemnet is true then compieler not checkes
    //remaining coditions
}
